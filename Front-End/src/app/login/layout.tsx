export const metadata = {
  title: 'Login — MTM QPR System',
  description:
    'Masuk ke QPR System PT Menara Terus Makmur (Astra Otoparts Group).',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
